node_modules/
build/
dist/
coverage/
.DS_Store
*.log
logs/
.env*
!.env.example

# Runtime state: the SQLite database and every captured photo. Never commit
# this - it is the store's actual catalogue work, and it belongs on a backed-up
# volume, not in git.
data-store/
